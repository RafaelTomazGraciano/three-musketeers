#include <stdio.tm>

struct Person {
    string name,
    int age
};

int main() {
    string name;
    Person p;
    
    gets(name);
    gets(p.name);
    printf("Name: %s", name);
    printf("Struct Name: %s", p.name);
    
    return 0;
}